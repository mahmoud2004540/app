import { defineModule } from '../defineModule';

export const honorModule = defineModule({
  id: 'honor',
  displayName: 'Honor',
  kind: 'brand',
  recommendedTools: ['hisuite'],
  aliases: ['honor'],
});
