import { defineModule } from '../defineModule';

export const oneplusModule = defineModule({
  id: 'oneplus',
  displayName: 'OnePlus',
  kind: 'brand',
  recommendedTools: [],
  aliases: ['oneplus'],
});
