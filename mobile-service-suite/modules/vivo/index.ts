import { defineModule } from '../defineModule';

export const vivoModule = defineModule({
  id: 'vivo',
  displayName: 'Vivo',
  kind: 'brand',
  recommendedTools: [],
  aliases: ['vivo', 'iqoo'],
});
